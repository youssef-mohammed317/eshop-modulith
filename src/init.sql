-- Create the identity schema for Keycloak
CREATE SCHEMA IF NOT EXISTS identity AUTHORIZATION postgres;

-- Create the ordering schema for the Ordering microservice
CREATE SCHEMA IF NOT EXISTS ordering AUTHORIZATION postgres;